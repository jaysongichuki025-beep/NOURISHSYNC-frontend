version: '3.8'

services:
  backend:
    build: ./NOURISHSYNC-backend
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development

  frontend:
    build: ./NOURISHSYNC-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend